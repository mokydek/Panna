# Тест-план

## Честный статус

Многопользовательские тесты Roblox Studio нельзя считать пройденными только по статике, сборке или Edit `RunScript`. Текущий Studio CLI smoke `0.3.0-alpha` подтверждает структуру BrightBlock-сцены и чистые контракты механик/VFX, но не шагает обычный client Play lifecycle и не заменяет Local Server. Точные границы зафиксированы в [фактических проверках](TESTING.md); документированный multiplayer-прогон всё ещё нужен.

Текущий trajectory/visual/animation pipeline прошёл canonical bake, статические проверки и Studio CLI smoke: подтверждены структура дуг, drag, roll, панели, flight-only trail, четыре конфигурации физического финта, procedural-позы, scoped action cues и cleanup. Фактическое ощущение мяча и плавность игрока всё ещё должны пройти сценарии `B-12`…`B-17` в Play.

Фиксируйте для каждого прогона дату, commit SHA, версию Studio, число клиентов, платформу, результат и ссылку на журнал/видео дефекта.

## Уровни проверки

1. **Структура:** `scripts/validate-project.ps1`, затем `rojo build`.
2. **Статика:** `stylua --check src` и `selene src`, если инструменты установлены.
3. **Bake/visual review:** запечённая единственная сцена, дневной свет, покрытия, разметка и читаемость.
4. **Play Solo:** загрузка сервера, мира, одного клиента, физическое `Controlled`/action transitions и lifecycle reset без необработанных ошибок.
5. **Local Server:** минимум два клиента, полный цикл 1v1 и два матча подряд.
6. **Опубликованный тестовый Experience:** DataStore, реальные устройства и сетевые условия.

Ни один уровень не заменяет следующий.

## Подготовка

- используйте отдельный тестовый Experience;
- синхронизируйте текущий commit через Rojo;
- очистите Output перед тестом;
- убедитесь, что открыт canonical `default.project.json` и одна сцена `PannaDistrict`, а не отдельная копия карты;
- для DataStore заранее решите, какие тестовые профили допустимо менять;
- не включайте API Services у production Experience;
- запускайте минимум два клиента; для полной проверки параллельности шести комнат потребуется 12 клиентов, а тринадцатый проверяет ожидание при полной загрузке.

## Матрица функциональных сценариев

| ID | Сценарий | Действия | Ожидаемый результат |
| --- | --- | --- | --- |
| S-01 | Холодный старт | Запустить сервер без сохранённых данных | Мир создан один раз, клиент получает snapshot, необработанных ошибок нет |
| V-01 | Яркая минимальная сцена | Открыть canonical place в Edit Mode и Play на низкой/высокой графике | Один `PannaDistrict`, `DistrictStyle = BrightBlockDistrict`, `FieldStyle = BrightBlockFootballV1`; Bloom/SunRays/глобальные тени выключены, мяч, ворота, prompts и HUD читаются плоскими цветами |
| V-02 | Покрытия и разметка | Обойти все шесть комнат и сверить Court/PitchFinish/markings | Каждый Court — зелёный Grass с одинаковой физикой и полосами покоса; видны touch/goal/center/penalty lines и круг; в мире нет BasePart с Material Neon, Parts не мешают мячу/персонажу |
| Q-01 | Вход в очередь | Игрок A входит один | Атрибут/UI показывают ожидание, матч не стартует |
| Q-02 | Отмена | A выходит из очереди | A удалён ровно один раз, следующий игрок не получает «призрачную» пару |
| Q-03 | Повторный запрос | Быстро нажать вход несколько раз | В очереди остаётся одна запись, сервер не создаёт два матча |
| Q-04 | Персонаж не готов | Убить/удалить Character и запросить очередь до respawn | Запрос отклонён с сообщением; арена не резервируется |
| M-01 | Полный цикл | A и B входят в одну комнату или общую очередь | Выдана свободная арена, countdown, active, result, возврат к `StreetSpawn` комнаты |
| M-02 | Блокировка старта | Во время countdown нажимать действия | Сервер не применяет действия до Active |
| M-03 | Полная сфера за линией | Провести центр мяча из поля дальше `radius + epsilon` в створе | Только после полного пересечения сервер один раз увеличивает правильную сторону, показывает pause и делает reset |
| M-04 | Касание и половина | Последовательно коснуться линией передней частью сферы, поставить центр на плоскость и оставить часть мяча на поле | Ни касание, ни половина мяча не дают гол и не включают debounce |
| M-05 | Лимит очков | Достичь настроенного лимита голами/паннами | Матч завершается один раз до конца таймера |
| M-06 | Ничья | Закончить основное время с равным счётом | Запускается overtime согласно Config |
| M-07 | Golden goal | Забить в overtime | При включённой настройке матч сразу завершается |
| M-08 | Выход в countdown | Отключить B до старта | Матч `Cancelled`, наград/рейтинга нет, A возвращается, арена освобождается |
| M-09 | Ранний выход | Отключить B раньше 15 секунд Active | `EarlyForfeit` показывает результат, но не выдаёт награды/рейтинг |
| M-10 | Поздний выход | Отключить B после 15 секунд Active | `Forfeit` показывает техническую победу A, но ни A, ни B не получают монеты, XP, статистику или рейтинг; арена освобождается |
| M-11 | Выход во время очереди | Отключить ожидающего игрока | Запись удаляется, другой игрок не блокируется |
| M-12 | Повтор цикла | После результата снова войти в очередь | Новый матч стартует без старых таймеров, мяча и соединений |
| M-13 | Граница таймера | Создать гол/панну сразу после точного `EndsAt`, до следующего timer poll | Событие не меняет завершившийся период |
| M-14 | Нет Character | Удалить/убить Character после входа в очередь до pairing и отдельно во время countdown | Неготовый игрок отбрасывается до reserve; перед `Active` readiness/teleport отменяет уже созданный матч без награды |
| M-15 | Disconnect на дедлайне | Отключить B сразу после `EndsAt`, но до следующего timer poll | Сервер завершает матч обычным `Time`/`Draw`, а не превращает истёкший период в `Forfeit` |
| M-16 | Swept быстрый гол | За один Heartbeat переместить мяч из поля глубоко за линию через центр створа | Интерполированная full-sphere точка находится, начисляется ровно один гол без tunneling |
| M-17 | Стойки/перекладина/задняя сторона | Пересечь плоскость рядом со стойкой, с касанием стойки/перекладины и отдельно войти со стороны сетки к полю | Все варианты отклонены; signed distance должна увеличиваться только для направления поле → ворота |
| M-18 | Управление после матча | Завершить матч каждым путём, дождаться cleanup, двигаться/прыгать/вращать камеру, затем сыграть второй матч | `ControlsLocked = false`, восстановлены Humanoid/PlayerModule/camera/HUD/input; второй матч стартует без зависшего charge/Shield/Trap/Dash/pending state |
| M-19 | Пауза во время hold-анимации | Начать Charge и Shield, заморозить/свернуть клиент дольше пяти секунд, затем доставить серверную `Stop` и отдельно имитировать её потерю | Запоздалая scoped `Stop` всё равно снимает hold; без неё локальный hard-timeout восстанавливает joints не позднее шести секунд |
| R-01 | Первый входит в комнату | A активирует `EntryPrompt` свободной комнаты | A перемещён на `HomeWaitingSpawn`; состояние `Waiting`, `WaitingCount = 1`, табло обновлено |
| R-02 | Второй входит в комнату | B активирует `EntryPrompt` той же комнаты | Начинается один матч A–B; комната проходит `Countdown` и `Active`, барьер закрыт |
| R-03 | Выход из ожидания | A активирует `ExitPrompt` до прихода B | A возвращён на `StreetSpawn`; комната снова `Free`, prompt доступен, барьер открыт |
| R-04 | Timeout ожидания | Оставить A одного дольше `WaitingTimeoutSeconds` | Ожидание очищено один раз, A возвращён на улицу, комната не остаётся занятой |
| R-05 | Занятая комната | C пытается войти во время `Countdown`/`Active`/`Result` | Вход отклонён, текущий матч и табло не изменены |
| R-06 | Смена комнаты | A ждёт в Arena_1 и пытается войти в Arena_2 | Второй выбор отклонён до явного выхода из первой комнаты |
| R-07 | Полный переход состояний | Провести матч через выбранную комнату | `Free → Waiting → Countdown → Active → Result → Free`; attrs, prompts, барьер и табло согласованы на каждом этапе |
| A-01 | Шесть комнат | Запустить 12 клиентов и сформировать шесть пар | Все шесть матчей используют разные ArenaId и не меняют чужой счёт/мяч |
| A-02 | Все комнаты заняты | Добавить тринадцатого/четырнадцатого клиента в общую очередь | Игроки остаются в очереди до освобождения комнаты |
| A-03 | Изоляция арен | Участником Arena_2 и spectator касаться мяча/участников Arena_1; войти в её занятый Bounds | Чужие группы не сталкиваются с мячом или игроками матча; посторонний возвращается на улицу и получает `ArenaIntrusionCount` |
| B-01 | Падение мяча | Сбросить мяч ниже разрешённого Y | Сервер возвращает мяч в корректную точку |
| B-02 | Слишком высокая скорость | Создать аномальную скорость в тестовой среде | Сервер ограничивает/восстанавливает состояние без гола |
| B-03 | Длительность рывка | Нажать Dash и измерить горизонтальную скорость по Heartbeat | `DashSpeed` поддерживается около `DashSeconds`, затем горизонтальная скорость ограничивается обычным envelope; вертикальная скорость сохраняется |
| B-04 | Невозможное перемещение | Телепортировать участника внутри/вне Bounds один и три раза за четыре секунды | Скачок откатывается к trusted CFrame, действие отклоняется; после третьего нарушения игрок возвращается на spawn |
| B-05 | Контракт player-ball | Участник вбегает в мяч без `ActionRequest`, затем отправляет действие внутри и вне допустимой серверной геометрии | Физическое столкновение персонажа не сообщает мячу импульс; сервер принимает только геометрически допустимое действие и отклоняет недопустимое |
| B-06 | Physical Controlled | Получить владение, стоять, идти, спринтовать и поворачиваться на 180° | Мяч всегда `Anchored = false`, network owner — сервер; `PannaDribbleForce`/`PannaRollTorque` включены только в `Controlled`, сфера физически догоняет цель без teleport/CFrame-follow, остаётся перед игроком и естественно катится без устойчивой дрожи |
| B-07 | Spherecast obstacle guard | С мячом идти прямо в борт, стойку и узкий угол | `Spherecast` сокращает физическую цель с clearance до препятствия, не записывает transform, мяч не проходит сквозь геометрию и безопасно теряет контроль при длительной блокировке/недоступной поверхности |
| B-08 | Physical action matrix | Из `Controlled` выполнить Low/Normal/Chip, Pass, Tackle и Panna; отдельно вызвать loss, death, reset, finish и detach | Всегда `Anchored = false` и network owner `nil`; Kick/Pass/Panna выключают force/torque для release, Tackle напрямую меняет controller и сохраняет физический PD-контроль, reset возвращает на `BallSpawn` и обнуляет скорости |
| B-09 | Физический первый приём | Принять сильный пас с Trap, затем тот же пас без Trap | Trap гасит больше относительной скорости, а автоприём сохраняет небольшое движение вперёд; оба используют ограниченный импульс без записи velocity/CFrame, а слишком быстрый мяч не приклеивается |
| B-10 | Spin и Magnus | Выполнить одинаковые удары без spin, с левым и правым spin; повторить на земле и в полёте | Без spin нет бокового ускорения; противоположный spin даёт ограниченное отклонение в разные стороны, в полёте эффект сильнее, скорости остаются конечными и внутри caps |
| B-11 | Меньший controllable ball | Проверить семь сфер после bake, затем ведение стоя/шагом/спринтом, смену направления на 180°, первый приём пасов разной силы и Low/Normal/Chip у стен/стоек | Диаметр везде `2.3` stud, масса близка к прежней, trail/spawn/goal используют `Radius = 1.15`; мяч остаётся близко без дрожи, телепорта, чрезмерного магнитного перехвата или прохода сквозь препятствие |
| B-12 | Реальные дуги | С максимальным зарядом на ровном поле измерить время в воздухе, вершину и горизонтальную дальность Low/Normal/Chip/Pass до первого касания | Порядок высоты `Pass < Low < Normal < Chip`; ориентиры без drag/carry/collision: Low `0.061 s / 0.09 / 4.8`, Normal `0.275 s / 1.86 / 23.1`, Chip `0.499 s / 6.12 / 30.0`, Pass `0.018 s / 0.008 / 1.03`; реальные дальности конечны и drag их уменьшает |
| B-13 | Speed-derived roll | Выполнить одинаковые действия на разных скоростях, записать горизонтальную скорость и угловую скорость, снять slow-motion вращения панелей | Roll следует `speed / radius × RollRatio`, направление соответствует движению, Chip имеет небольшой backspin; панели вращаются вместе со сферой без плоского скольжения «тарелкой» |
| B-14 | Drag, state и trail | Повторить быстрый удар в воздухе и по земле, наблюдать скорость, grounded, `BallState` и `PannaSpeedTrail.Enabled` около порогов `68/60` | В воздухе скорость плавно снижается ограниченным drag; trail тонкий и нейтральный, включён только у негрунтового `Shot/Flight/Bounce`; на земле он выключен, а наземный `Shot` уходит в `Free` |
| B-15 | Панели и visual repair | После bake проверить семь мячей, затем удалить/испортить одну панель и повторно запустить builder | Корень и мячи имеют актуальный `BallVisualVersion`; у каждого мяча ровно шесть `PannaBallPanel_01`…`06`, они massless/non-colliding/non-touching/non-querying и welded; builder восстанавливает контракт без второго района |
| B-16 | Плавная дистанция ведения | Плавно разогнаться от нуля до спринта и обратно | Мяч без скачка положения плавно меняет цель между `2.15`, `2.55` и `3.35` stud; нет заметной дрожи на порогах |
| B-17 | Ограниченная помощь удара/паса | Ударить немного рядом со створом, затем явно в сторону/назад; повторить пас в беге | Первый удар слегка корректируется, явно неверный — нет; пас только слегка согласуется с доверенной линией движения; ни один assist не выбирает игрока-цель |
| FX-01 | Точное направление ввода | С владением повернуть камеру/стик по восьми направлениям на keyboard/mouse, gamepad и touch, сравнить стрелку с отправленным `ActionRequest.direction` | Стрелка совпадает с нормализованным горизонтальным payload; смена типа удара не поворачивает её самостоятельно |
| FX-02 | Заряд и приблизительная траектория | Удерживать Kick/Pass на 10%, 50%, 100%, переключить Low/Normal/Chip | Длина/толщина стрелки и точки плавно меняются; цвет/дуга читаемо различают режимы, а VFX не пишет `CFrame`/velocity мяча |
| FX-03 | Фактическое движение | Отпустить Low/Normal/Chip/Pass и наблюдать стрелку при отскоке/замедлении | Aim guide исчезает после потери владения; при достаточной скорости motion guide следует текущему `AssemblyLinearVelocity`, меняется с физикой и исчезает ниже порога |
| FX-04 | Scoped action VFX | Выполнить Kick, Pass, Trap, Shield Start/Stop, Dash, Feint, Tackle Miss/Blocked/Standing, Skill/Panna и Goal в двух аренах | Клиент показывает только scoped события своего контекста; эффекты соответствуют действию, не создают collision/query/touch и не влияют на результат |
| FX-05 | Бюджет и lifecycle | Быстро вызвать больше `MaximumTransientParts`, затем завершить матч, сменить Character и уничтожить client script | Старые части удаляются первыми; guides, pulses, streaks и shield Highlight полностью очищаются без дубликатов `PannaLocalVFX_*` |
| P-01 | Кандидат на панну | Провести мяч через область между ног соперника и сохранить владение | Срабатывает одна серверная панна только при полном наборе условий |
| P-02 | Ложная панна | Потерять владение/пройти сбоку/быть слишком далеко | Панна не засчитывается |
| P-03 | Очко панны | Выполнить панну при счёте в одном очке от лимита и отдельно в overtime | Табло получает `PannaPoints`; матч завершается по лимиту или как GoldenPanna ровно один раз |
| P-04 | Защитник перехватил | После пересечения отдать владение защитнику, повернуть его и позже вернуть мяч атакующему | Кандидат отменён при чужом owner/last touch; панна не засчитывается |
| P-05 | Движущийся защитник до прохода | После начала финта, но до пересечения значительно переместить защитника или заменить его Character | Кандидат отменяется; Begin-time плоскость/створ не переносятся к новому положению и ложная панна не засчитывается |
| P-06 | Реакция после прохода | После корректного пересечения защитник отходит, затем атакующий вовремя возвращает владение без чужого касания | Обычное движение защитника не отменяет уже подтверждённый проход; панна засчитывается один раз |
| P-07 | Поза защитника и assist | Попытаться панна-финт против защитника, смотрящего на атакующего, боком и спиной; сместить прицел внутри и за пределы конуса | Спиной и за пределами конуса попытка отклоняется; допустимая попытка направляется в зафиксированный коридор |

| T-01 | Отбор спереди/сбоку/сзади | В дистанции контакта повторить отбор из трёх позиций | Спереди и допустимый бок работают; явная спина даёт `BadTackleAngle` |
| T-02 | Цена неудачной попытки | В контакте атаковать под плохим углом и в активный Shield, сразу повторить | Первая попытка показывает Miss/Blocked и тратит `0.9 s`; повтор возвращает `Cooldown` |
| T-03 | Прямое владение после отбора | При владении соперника нажать ЛКМ, затем повторить через `E`/`B`/`TACKLE` в допустимом переднем и боковом секторе | ЛКМ отправляет `Tackle`, а не `ChargeStart/Kick`; `OwnerUserId` и `Controlled` сразу переходят отбирающему без release-пинка, скорость/вращение гасятся ограниченным импульсом, прежний владелец не получает пассивный recapture в grace-окне |
| T-04 | Четыре физические траектории финта | На разных скоростях выполнить StepOver, Cut в обе стороны, DragBack и Roulette в обе стороны | Мяч остаётся unanchored/server-owned; StepOver описывает возвращающуюся боковую дугу, Cut уходит в выбранную сторону, DragBack движется назад, Roulette разворачивает направление по дуге; ни один вариант не пишет `CFrame`, а окно заканчивается через `0.42…0.58 s` или дальше `5.8` stud |
| T-05 | Защита траектории финта | Соперник выполняет `TACKLE` и подходит для пассивного capture во время каждого варианта, затем повторяет сразу после окна | Во время короткой траектории владелец не меняется, feedback — `DribbleProtected`, cooldown отбора тратится; после окна чистый отбор снова возможен |
| D-01 | Первичная загрузка | Новый игрок входит в тестовый Experience | Создаётся профиль со значениями по умолчанию, `DataLoaded` корректен |
| D-02 | Повторный вход | Изменить статистику легальным матчем и переподключиться | Подтверждённые данные восстанавливаются |
| D-03 | Ошибка DataStore | Имитировать недоступность в безопасной тестовой среде | Нет crash и перезаписи живого профиля пустыми данными |
| D-04 | Идемпотентность | Попытаться завершить один матч несколькими событиями | Награда и статистика применены один раз |
| D-05 | Session lock | Имитировать живой lock другого session ID и затем истёкший lock | Живой lock не перезаписывается и даёт безопасный fallback; истёкший можно атомарно приобрести |
| D-06 | Конкурентные save | Совместить autosave, выход и shutdown в тестовой обвязке | Per-player guard сериализует записи, чистое завершение освобождает lock в пределах budget |
| D-07 | Stale save после takeover | Дать lease A истечь, загрузить/освободить профиль с B, затем позволить A сохранить старую копию | A получает отказ даже при уже пустом lock; stale-данные не записываются без exact ownership |
| D-08 | Disconnect при load/save | Отключиться сразу после захвата lease и отдельно во время занятого autosave | Завершившийся load или сериализованный final save освобождает lock контролируемо |
| D-09 | Fallback и рейтинг | В published test войти с `DataSessionFallback = true` и запросить очередь/завершить матч; повторить в Studio | Published ranked блокируется; Studio разрешает матч только для разработки, но `RewardMatch` не применяет несохраняемый прогресс |
| D-10 | Быстрый rejoin того же сервера | Перезайти тем же UserId до завершения позднего release старого `Player` | Каждый load имеет новый lease ID; старый объект не сохраняет данные и не снимает lock новой сессии |
| D-11 | Потеря подтверждённого save | После успешной загрузки имитировать transport/ownership failure autosave, затем восстановить запись | Сессия становится `DataSessionFallback`, ranked/награды блокируются; только успешный exact-owner save снимает unsafe-состояние |
| D-12 | Shutdown во время load/save | Начать медленный load и save, затем вызвать shutdown | Новые load не стартуют; активные loading/saving ожидаются в пределах общего budget, поздний lock не остаётся без попытки release |
| D-13 | Выход fallback-игрока | Отключить игрока после неуспешной загрузки без приобретённого lease | Очистка завершается без tight loop/заморозки серверного потока |

## Ввод и интерфейс

Проверьте отдельно:

- клавиатуру и мышь;
- основные бинды: ЛКМ заряжает удар только при своём владении и выполняет отбор при владении соперника, ПКМ — низкий пас, `Q` — дриблинг/финт, `E` — отдельный отбор; отпускание ЛКМ после попытки отбора не запускает Kick;
- геймпад с корректными подсказками текущего устройства;
- телефон в портретной и альбомной ориентации;
- планшет;
- разрешение с safe-area/notch;
- смену устройства ввода во время матча;
- полноту guide: Ball Control, Shot, Shot Type, Pass, Dribble Move, Panna, Tackle, Shield/First Touch и Dash показывают правильные keyboard/mouse, gamepad и touch bindings;
- открытие/закрытие guide через **HELP**, `H`, `Select`, touch `?`, close и `Escape`; первый `Countdown` показывает его один раз, `Active` закрывает, а второй матч не открывает повторно;
- modal guide во время charge/Shield/Trap: игровые команды не уходят, удержания очищаются, после закрытия управление сразу доступно;
- прокрутку и отсутствие выхода панели за safe-area при desktop, gamepad, portrait/landscape phone и tablet; смена PreferredInput не создаёт дубликаты HUD;
- отсутствие перекрытия счёта, таймера и мобильных кнопок;
- читаемость countdown, гола, панны, победы и поражения;
- читаемость белой/голубой/зелёной/жёлтой/розовой VFX-палитры на всех шести покрытиях и при режимах качества 1/5/10;
- отсутствие перекрытия мяча/игрока слишком крупными импульсами и стабильный FPS при повторных ударах на слабом мобильном устройстве;
- восстановление HUD после второго матча без дубликатов;
- отмену удерживаемых shot/pass/shield при `ControlsLocked`, окончании, выходе, смерти и respawn;
- восстановление `CameraType`/`CameraSubject` и Roblox PlayerModule controls после каждого lifecycle reset.

## Adversarial-проверки

Проводите только в собственном тестовом Experience.

| ID | Запрос | Ожидаемая реакция сервера |
| --- | --- | --- |
| X-01 | Неизвестное имя действия | Отклонить без изменения состояния |
| X-02 | Строка вместо Vector3/таблицы | Отклонить без исключения обработчика |
| X-03 | NaN, infinity, нулевое/огромное направление | Отклонить или безопасно нормализовать по контракту |
| X-04 | Сотни Remote в секунду | Ограничить; сервер и честные игроки продолжают работать |
| X-05 | Удар из лобби или чужой арены | Отклонить |
| X-06 | Действие дальше допустимого радиуса мяча | Отклонить |
| X-07 | Клиентская попытка назначить счёт/монеты | У клиента нет такого доверенного пути |
| X-08 | QueueRequest во время матча | Отклонить без разрушения текущего матча |
| X-09 | Client-owned персонаж мгновенно телепортируется к мячу внутри bounds и отправляет действие | Серверная история выявляет невозможный скачок, откатывает позицию и отклоняет действие; повторные нарушения возвращают на spawn |
| X-10 | Третий игрок входит в занятую арену и пытается столкнуться с мячом | Spectator collision group не взаимодействует с мячом, scan возвращает игрока к `StreetSpawn` |
| X-11 | В тестовой обвязке имитировать ошибку регистрации collision groups | Сервер пишет предупреждение и продолжает периодический eject без crash |
| X-12 | Клиент пытается изменить transform/`Anchored`/owner контролируемого мяча или присылает произвольную цель/силу | Сервер восстанавливает `Anchored = false` и owner `nil`; Remote не принимает позицию, target, `VectorForce`, `Torque` или Magnus-ускорение, а серверный физический state остаётся источником истины |

## Производительность и длительный прогон

1. Запустите последовательность минимум из 20 коротких матчей.
2. Сравните число активных Instances и соединений в начале и конце.
3. Проверьте MicroProfiler на лишнюю работу каждый кадр.
4. На слабом мобильном устройстве пройдите всю улицу и проверьте все шесть комнат при `StreamingEnabled`.
5. Убедитесь, что временные эффекты и старые мячи удаляются.
6. Проверьте сервер с максимальным целевым числом игроков.
7. Сравните GPU/frame time дневных Atmosphere/ColorCorrection/Bloom/SunRays и геометрии полос/разметки на минимальном и максимальном качестве.

Зафиксируйте фактические FPS, server frame time и потребление памяти; не используйте непроверенные цифры в описании игры.

## Критерии приёмки первого MVP

Версию можно пометить как проверенный MVP, только если два игрока не менее трёх раз подряд могут:

1. загрузиться в одном лобби;
2. выбрать комнату через `EntryPrompt` (и отдельно проверить общую очередь);
3. дождаться соперника и получить выбранную арену;
4. использовать заявленные футбольные действия;
5. забить серверно подтверждённый гол;
6. завершить основное или дополнительное время;
7. увидеть одинаковый результат;
8. получить награду ровно один раз;
9. вернуться к уличной точке комнаты;
10. свободно двигаться, прыгать и управлять камерой после cleanup, затем начать следующий матч без перезапуска клиента;
11. переподключиться и восстановить сохранённую статистику.

Отдельно должны пройти V-01…V-02, FX-01…FX-05, M-03…M-04 и M-16…M-18, B-06…B-17, R-01…R-07, X-01…X-12, D-05…D-13, изоляция шести параллельных комнат и ненаграждаемые disconnect-сценарии. Любой разрыв с ожидаемым результатом является `NO-GO` для рейтингового публичного запуска, пока не исправлен и не превращён в regression test.

## Шаблон отчёта

```text
Commit:
Studio version:
Дата и окружение:
Клиенты/устройства:
Сценарии:
Пройдено:
Не пройдено:
Ошибки Output:
Ссылки на видео/логи:
Решение о выпуске: GO / NO-GO
Ответственный:
```
